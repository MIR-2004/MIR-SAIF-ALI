# Resend (https://resend.com) – required for contact form
# Create an API key at https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxx

# Optional: override default sender (must be a verified domain in Resend)
# RESEND_FROM_EMAIL=Portfolio <hello@yourdomain.com>

# Optional: override where contact form emails are sent (default: mir.saif.ali2004@gmail.com)
# RESEND_TO_EMAIL=you@example.com

# Local dev: run "npx vercel dev" (not "npm run dev") so /api/contact is available.
# On Vercel, set RESEND_API_KEY in Project Settings → Environment Variables.
